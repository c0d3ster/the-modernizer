{
  "name": "the-modernizer",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "modernize": "tsx apps/cli/src/index.ts",
    "generate-compare": "bash scripts/compare-generators.sh"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "tsx": "^4.19.2",
    "turbo": "^2.9.3",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
