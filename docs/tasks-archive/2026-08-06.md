# Archived Tasks — 2026-08-06

## #1 [stack: discovery] Implement static HTML scoring

Implement static HTML scoring per docs/market-discovery.md "Sub-score 1: Static HTML". All signals from the weights table (SSL, viewport, old jQuery, old WP theme, table layout, OG tags, IE compat), normalized via the documented formula.

- **PR:** [#19](https://github.com/c0d3ster/the-modernizer/pull/19) — `overnight/2026-08-06/01-static-html-scoring` → `main`
- **Notes:** New `packages/discovery` package created (mirrors `packages/crawler` scaffold). SSL and staleness weight are accepted as injected inputs to `computeStaticScore` rather than computed from HTML, since neither is derivable from a raw HTML string alone. Table-layout heuristic (undocumented in the spec): a `<table>` is "layout" when it has no `<th>`, no `<thead>`, and no `role="table"`. Added a `.gitignore` exception for `packages/discovery/fixtures/` since the repo-wide `**/fixtures/` rule is meant for live-crawl output, not these hand-authored, deterministic golden-formula fixtures.

## #2 [stack: discovery] Implement staleness detection via Wayback CDX

Implement staleness detection via Wayback CDX per docs/market-discovery.md "Determining True Last-Updated Date". Use collapse=digest, sliding scale weight min(years × 4, 20), copyright-regex fallback worth 10 when Wayback has no data, 1 req/sec rate limit.

- **PR:** [#20](https://github.com/c0d3ster/the-modernizer/pull/20) — `overnight/2026-08-06/02-staleness-detection` → `overnight/2026-08-06/01-static-html-scoring`
- **Notes:** Added `src/wayback-cdx.ts`, `src/rate-limiter.ts` (generic, reusable — no shared rate-limiter existed before), and `src/staleness.ts` to `packages/discovery`. Resolved fractional-year rounding (use continuous fraction, not rounded — matches the doc's own boundary values and its "< 1 year: 0-4" row) and single-CDX-snapshot behavior (treat identically to the multi-snapshot case; ambiguity resolves toward underestimating staleness, the safe direction). This PR targets task #1's branch, not `main`, since it's the next task in the `discovery` stack chain.
