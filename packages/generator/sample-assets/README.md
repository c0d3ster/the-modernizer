# Sample static assets

Files here are copied into the generated site’s `public/` folder on each `generateSite` run, so they are served at the site root (for example `/sample-hero.jpg`).

Add images referenced by `backgroundImageUrl`, Open Graph images, or other schema fields here so regenerating the output directory does not drop them.
